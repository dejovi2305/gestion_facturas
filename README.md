# Gestión de la factura

1. python -m venv venv

- Windows
  venv\Scripts\activate

2. pip install -r requirements.txt

3. pip install PyInstaller

4. python build_exe.py

5. Si se quiere armar el .exe para que se instale en el sistema: Instala Inno Setup: https://jrsoftware.org/isinfo.php
